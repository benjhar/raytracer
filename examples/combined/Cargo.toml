[package]
name = "combined"
version = "0.1.0"
edition = "2021"

[dependencies]
image = "0.25.5"
rand = "0.8.5"
raytracer = { path="../../raytracer/" }
linalg = { git="https://github.com/benjhar/linalg.git" }
